---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Set up base UI components and main layout for the bandwidth quoting platform.

Purpose: Establish the visual foundation and component library that all user-facing features will use.
Output: Working shadcn/ui setup, main app layout with navigation, and basic page structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**UI decisions:**
- shadcn/ui for component library (accessible, customizable, not a dependency)
- Clean, professional look suitable for B2B quoting tool
- Responsive design for desktop-first (sales/business users)

**Pages needed for foundation:**
- Home/Landing: Quote request entry point
- Dashboard: View leads and quotes (placeholder for now)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure shadcn/ui</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/*.tsx</files>
  <action>
1. Initialize shadcn/ui: `npx shadcn@latest init`
   - Style: Default
   - Base color: Slate
   - CSS variables: Yes
   - React Server Components: Yes
   - Write to: components (default)

2. Install essential components for a quoting app:
```bash
npx shadcn@latest add button
npx shadcn@latest add input
npx shadcn@latest add label
npx shadcn@latest add card
npx shadcn@latest add form
npx shadcn@latest add select
npx shadcn@latest add toast
npx shadcn@latest add sonner
npx shadcn@latest add table
npx shadcn@latest add badge
npx shadcn@latest add progress
```

3. Verify src/lib/utils.ts exists with cn() helper function.
  </action>
  <verify>Components exist in src/components/ui/, import { Button } from "@/components/ui/button" works in a test file</verify>
  <done>shadcn/ui initialized, 10+ base components installed, utils.ts configured</done>
</task>

<task type="auto">
  <name>Task 2: Create main app layout with navigation</name>
  <files>src/app/layout.tsx, src/components/layout/header.tsx, src/components/layout/footer.tsx, src/app/globals.css</files>
  <action>
1. Update src/app/layout.tsx with:
   - Toaster from sonner for notifications
   - Clean body structure with header/main/footer regions
   - Inter font (or system font stack)

2. Create src/components/layout/header.tsx:
   - Logo/brand: "Business Bandwidth AI" or "BandwidthQuote"
   - Navigation: Home, Dashboard (link to /dashboard)
   - Clean, professional header with subtle bottom border

3. Create src/components/layout/footer.tsx:
   - Simple footer with copyright
   - "Powered by Momentum Telecom" mention (optional)

4. Update src/app/globals.css:
   - Keep Tailwind directives
   - Add any custom CSS variables for brand colors if needed

5. Create src/app/page.tsx as landing page:
   - Hero section: "Get instant quotes for business internet"
   - CTA button linking to quote form (placeholder for Phase 2)
   - Brief value props: "200+ carriers", "2-3 minute quotes", "Business addresses only"

6. Create src/app/dashboard/page.tsx:
   - Placeholder dashboard page
   - Card showing "Leads" count (0 for now)
   - Card showing "Quotes" count (0 for now)
   - "Quote history will appear here" message
  </action>
  <verify>npm run dev shows layout on all pages, navigation between / and /dashboard works</verify>
  <done>App layout created with header, footer, home page hero, and dashboard placeholder</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Base UI layout with navigation, landing page, and dashboard placeholder</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. Verify: Header shows brand name and navigation links
    4. Verify: Landing page has hero section with value props
    5. Click: "Dashboard" in navigation
    6. Verify: Dashboard page shows placeholder cards
    7. Check: Footer appears on both pages
    8. Check: Overall look is clean and professional
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] All pages render without React errors
- [ ] Navigation works between pages
- [ ] Components import correctly from @/components/ui
</verification>

<success_criteria>
- shadcn/ui installed and configured
- 10+ base components available
- Main layout with header/footer
- Landing page with hero and CTA
- Dashboard placeholder page
- Navigation between pages works
- Human verified visual appearance
- Phase 1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`

Note: This completes Phase 1. Update STATE.md to reflect Phase 1 complete, ready for Phase 2.
</output>
